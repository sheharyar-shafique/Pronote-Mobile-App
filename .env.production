# Production API URL — points at the Render backend that the web app already uses.
# When the backend moves to AWS App Runner, update this value.
# iOS App Transport Security blocks cleartext HTTP, so this MUST be HTTPS.
VITE_API_URL=https://pronote-backend.onrender.com/api
VITE_USE_API=true
